import styled from 'styled-components/native';
import IPCONFIG from '../../config/IpConfig';
const RemittanceOneContainer = styled.SafeAreaView`
  flex: 1;
  background-color: white;
`;

export default RemittanceOneContainer;
