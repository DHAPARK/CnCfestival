import styled from 'styled-components/native';
import IPCONFIG from '../../config/IpConfig';
const RegistContents = styled.ScrollView`
  flex: 1;
  width: 95%;

  background-color: white;
`;

export default RegistContents;
