import styled from 'styled-components/native';
import IPCONFIG from '../../config/IpConfig';
const MarketMainDescriptionContainer = styled.SafeAreaView`
  flex: 1;
  background-color: white;
`;

export default MarketMainDescriptionContainer;
